For this problem we check first if a user is in the current group, or the current name. If yes,
we return true, if not, then we call the function again for every subgroup that we find in
the current level of the tree.
The runtime is O(n x levels of the groups)